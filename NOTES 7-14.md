# NOTES 7.14

## 1. Hero and Header
    - remove the outline and box shadow around the hero image, should just be the image
    - add a link on the header that says "shop the online store" and points to a collection of "online-store" products 
    - add the same link as a button next to the two buttons on the header 

## 2. Event+Membership split tile
 - this component will not be used anymore, the event content and calendar will be moved to it's own section.
 - we should create a new component that will be very similar to the component used here, except on the left side it will be a showcase of the products in the "online-store" collection.
 - the right side will contain the membership content and layout as it exists today
 - we will replace the existing component with the new one, but we will not delete the component


 ## 3. Events 
 
 - remove (but do not delete) the two promo tiles and replace it with a full width event component, which will be like the current event calendar but full width

 - we will create an event page that shows the full calendar of events

## 4. Staff picks

- we will only be pulling staff picks from the content section, and not from the products metadata at all

- staff picks should be renamed "community picks"
- the author section of each pick should have a link, which is input in the content piece 
- this section should be scrollable and show all the stack pick content items 

